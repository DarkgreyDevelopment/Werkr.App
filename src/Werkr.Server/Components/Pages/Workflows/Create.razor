@page "/workflows/create"
@attribute [Authorize( Roles = "Admin,Operator" )]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized( ) {
        Navigation.NavigateTo( "/workflows/new/dag-editor", replace: true );
    }
}
